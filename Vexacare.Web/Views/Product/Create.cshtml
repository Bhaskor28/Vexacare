@using Vexacare.Domain.Enums
@model Vexacare.Application.Products.ViewModels.AddProductVM
@{
    Layout = "_AdminLayout";
    ViewBag.Title = "Create Product";
}

@section Styles {
    <style>
        .file-upload-container {
            border: 2px dashed #dee2e6;
            padding: 20px;
            text-align: center;
            border-radius: 5px;
            margin-bottom: 20px;
        }

        .preview-image {
            max-height: 200px;
            margin-top: 10px;
        }

        select:invalid {
            color: #6c757d; /* Bootstrap's secondary color */
        }

        select option {
            color: #212529; /* Bootstrap's default text color */
        }
    </style>
}

<div class="container-fluid px-4">
    <h1 class="mt-4">Create Product</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a asp-controller="Admin" asp-action="Index">Dashboard</a></li>
        <li class="breadcrumb-item"><a asp-action="Index">Products</a></li>
        <li class="breadcrumb-item active">Create</li>
    </ol>

    <div class="card mb-4">
        <div class="card-header">
            <i class="fas fa-plus-circle me-1"></i>
            Product Details
        </div>
        <div class="card-body">
            <form asp-action="Create" method="post" enctype="multipart/form-data">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="row mb-3">
                    <div class="col-md-6">
                        <div class="form-floating">
                            <input asp-for="Name" class="form-control" placeholder="Product name" />
                            <label asp-for="Name"></label>
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-floating">
                            <input asp-for="Price" class="form-control" placeholder="Price" />
                            <label asp-for="Price"></label>
                            <span asp-validation-for="Price" class="text-danger"></span>
                        </div>
                    </div>
                </div>

                <div class="row mb-3">
                    <div class="col-md-6">
                        <div class="form-floating">
                            <select asp-for="ProductType" class="form-control"
                                    asp-items="Html.GetEnumSelectList<ProductType>()">
                                <option value="" selected disabled hidden>-- Select Type --</option>
                            </select>
                            <label asp-for="ProductType"></label>
                            <span asp-validation-for="ProductType" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-floating">
                            <label class="form-label">Product Image</label>
                            <div class="file-upload-container">
                                <input asp-for="ProductImage" type="file" class="form-control-file" id="imageUpload" accept="image/*" />
                                <div id="imagePreviewContainer" class="mt-2" style="display:none;">
                                    <img id="imagePreview" class="preview-image" />
                                </div>
                                <span asp-validation-for="ProductImage" class="text-danger"></span>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="mb-3">
                    <div class="form-floating">
                        <textarea asp-for="BreifDescription" class="form-control" placeholder="Breif Description" style="height: 100px"></textarea>
                        <label asp-for="BreifDescription"></label>
                        <span asp-validation-for="BreifDescription" class="text-danger"></span>
                    </div>
                </div>


                <div class="mb-3">
                    <div class="form-floating">
                        <textarea asp-for="Description" class="form-control" placeholder="Description" style="height: 100px"></textarea>
                        <label asp-for="Description"></label>
                        <span asp-validation-for="Description" class="text-danger"></span>
                    </div>
                </div>

                <div class="mb-4">
                    <label class="form-label">Product Benefits</label>
                    <div class="card">
                        <div class="card-body" style="max-height: 200px; overflow-y: auto;">
                            @for (int i = 0; i < Model.AvailableBenefits.Count; i++)
                            {
                                <div class="form-check">
                                    <input type="checkbox"
                                           id="benefit_@Model.AvailableBenefits[i].Id"
                                           name="SelectedBenefitIds"
                                           value="@Model.AvailableBenefits[i].Id"
                                           class="form-check-input" />
                                    <label class="form-check-label" for="benefit_@Model.AvailableBenefits[i].Id">
                                        @Model.AvailableBenefits[i].BenefitName
                                    </label>
                                </div>
                            }
                        </div>
                    </div>
                </div>

                <div class="d-flex justify-content-between">
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="fas fa-arrow-left me-1"></i> Back to List
                    </a>
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-save me-1"></i> Create Product
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            // Image preview functionality
            $('#imageUpload').change(function() {
                if (this.files && this.files[0]) {
                    var reader = new FileReader();
                    reader.onload = function(e) {
                        $('#imagePreview').attr('src', e.target.result);
                        $('#imagePreviewContainer').show();
                    }
                    reader.readAsDataURL(this.files[0]);
                }
            });

            // Handle form submission to ensure ProductType is selected
            $('form').submit(function(e) {
                if ($('#ProductType').val() === "") {
                    e.preventDefault();
                    $('[data-valmsg-for="ProductType"]').text("Please select a product type");
                }
            });
        });
    </script>
}